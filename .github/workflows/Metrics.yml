name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
            token: ${{ secrets.METRICS_TOKEN }}
            base: header
            plugin_isocalendar: yes
            plugin_languages: yes
            plugin_stars: yes
            plugin_lines: yes
            plugin_lines_sections: repositories, history
            config_order: base.header, isocalendar, languages, stars, lines
